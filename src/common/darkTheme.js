import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#802c6e',
        },
        secondary: {
            main: '#6e802c',
        },
        mode: 'dark'
    },
});

export default theme;